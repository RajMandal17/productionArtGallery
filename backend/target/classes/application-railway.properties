# Railway Production Configuration
spring.application.name=ArtworkEcommerce

# Railway Domain Information
application.railway.project.name=loving-illumination
application.railway.environment.name=Dev

# MySQL (Railway) - with connection resilience
spring.datasource.url=jdbc:mysql://${MYSQLHOST:mysql.railway.internal}:${MYSQLPORT:3306}/${MYSQL_DATABASE:railway}?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&autoReconnect=true&failOverReadOnly=false&maxReconnects=10
spring.datasource.username=${MYSQLUSER:root}
spring.datasource.password=${MYSQLPASSWORD:HPmzYslFXfpoVBmOQDipdLhfcBBkDmpD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql=false

# Database connection pool settings
spring.datasource.hikari.connection-timeout=60000
spring.datasource.hikari.initialization-fail-timeout=0
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000

# Redis (Railway) - with connection resilience
spring.redis.host=${REDISHOST:redis.railway.internal}
spring.redis.port=${REDISPORT:6379}
spring.redis.password=${REDISPASSWORD:SlVhZZoITyqaMAIYwudpSwmnRkviJYmH}
spring.redis.username=${REDISUSER:default}
spring.redis.timeout=10000ms
spring.redis.lettuce.pool.max-active=8
spring.redis.lettuce.pool.max-wait=-1ms
spring.redis.lettuce.pool.max-idle=8
spring.redis.lettuce.pool.min-idle=0

# JWT
jwt.secret=${JWT_SECRET:5e295a6faec1820d2d2a3f0d9d504cbc}
jwt.expiration=${JWT_EXPIRATION:3600000}
jwt.refresh.expiration=${JWT_REFRESH_EXPIRATION:604800000}

# Swagger
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# Email
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=rajmandal147@gmail.com
spring.mail.password=wxtwgpmkkzlrqmqx
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.from=rajmandal147@gmail.com

# File Upload (using relative path for Railway)
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=20MB
file.upload-dir=./uploads

# CORS - Configure for Railway frontend integration
cors.allowed-origins=https://artworkgallery-dev.up.railway.app,http://localhost:3000,http://localhost:5173

# Logging levels
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.com.artwork=DEBUG
logging.level.root=INFO

# Server port (Railway automatically assigns the PORT)
server.port=${PORT:8080}

# Management and Health Check endpoints
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always
management.health.db.enabled=true
management.health.redis.enabled=true
